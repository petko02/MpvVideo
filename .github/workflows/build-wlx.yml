name: Build WLX Plugin (MpvVideo)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          brew update
          brew install mpv coreutils

      - name: Download mpv headers from upstream
        run: |
          curl -L -o client.h https://raw.githubusercontent.com/mpv-player/mpv/master/libmpv/client.h
          curl -L -o opengl_cb.h https://raw.githubusercontent.com/mpv-player/mpv/master/libmpv/opengl_cb.h

      - name: Prepare plugin directory
        run: mkdir -p MpvVideo.wlx

      - name: Compile Objective-C++
        run: |
          PREFIX=$(brew --prefix mpv)
          clang++ -ObjC++ -Wall -O2 -fPIC \
            -I. \
            -I"${PREFIX}/include" \
            -L"${PREFIX}/lib" \
            -framework Cocoa \
            -framework QuartzCore \
            -framework OpenGL \
            mpvvideo.mm \
            -dynamiclib -o MpvVideo.wlx/libmpvvideo.dylib \
            -lmpv

      - name: Package for release
        run: |
          mkdir -p release/MpvVideo
          cp MpvVideo.wlx/libmpvvideo.dylib release/MpvVideo/
          cp *.h mpvvideo.mm README.md release/MpvVideo/ || true
          cd release && zip -r MpvVideo.zip MpvVideo

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MpvVideo
          path: release/MpvVideo.zip
