name: Build WLX Plugin for macOS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Create plugin folder
        run: mkdir -p MpvVideo.wlx

      - name: Compile WLX plugin (universal binary)
        run: |
          export CFLAGS="-arch arm64 -arch x86_64"
          export LDFLAGS="-arch arm64 -arch x86_64"
          clang++ -ObjC++ -Wall -O2 -fobjc-arc -fPIC \
            $CFLAGS \
            -framework Cocoa \
            -framework AVKit \
            -framework AVFoundation \
            mpvvideo.mm \
            -dynamiclib -o MpvVideo.wlx/MpvVideo.wlx \
            $LDFLAGS

      - name: Upload Plugin
        uses: actions/upload-artifact@v4
        with:
          name: MpvVideoPlugin
          path: MpvVideo.wlx/MpvVideo.wlx
