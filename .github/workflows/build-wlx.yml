name: Build WLX Plugin (MpvVideo)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🛠️ Install Dependencies
        run: |
          brew install fpc coreutils unzip curl

      - name: 🧱 Compile Cocoa Wrapper (libMpvPlayerView.dylib)
        run: |
          clang++ -dynamiclib -fobjc-arc -framework Cocoa \
            -o libMpvPlayerView.dylib MpvPlayerView.mm \
            -arch x86_64 -arch arm64

      - name: 🧠 Compile WLX Plugin (Free Pascal Universal Build)
        run: |
          SDKROOT=$(xcrun --sdk macosx --show-sdk-path)
          FRAMEWORKS="$SDKROOT/System/Library/Frameworks"

          fpc -Px86_64 \
              -XR"$SDKROOT"/usr/bin \
              -Fl"$SDKROOT"/usr/lib \
              -k"-F$FRAMEWORKS" \
              -k"-framework" -k"Foundation" \
              -Fu. -oMpvVideo_x64.wlx MpvVideo.pas

          fpc -PAARCH64 \
              -XR"$SDKROOT"/usr/bin \
              -Fl"$SDKROOT"/usr/lib \
              -k"-F$FRAMEWORKS" \
              -k"-framework" -k"Foundation" \
              -Fu. -oMpvVideo_arm64.wlx MpvVideo.pas

      - name: 🧬 Merge into Universal WLX Binary
        run: |
          lipo -create -output MpvVideo.wlx MpvVideo_x64.wlx MpvVideo_arm64.wlx
          file MpvVideo.wlx
          chmod +x MpvVideo.wlx

      - name: 📦 Bundle plugin files
        run: |
          mkdir -p MpvVideo_Package
          cp MpvVideo.wlx MpvVideo_Package/
          cp libMpvPlayerView.dylib MpvVideo_Package/

          # Optional: Include existing universal libmpv.dylib if present
          if [ -f libmpv.dylib ]; then
            cp libmpv.dylib MpvVideo_Package/
          fi
          if [ -f libmpv.2.dylib ]; then
            cp libmpv.2.dylib MpvVideo_Package/
          fi

      - name: 🗜️ Create ZIP archive
        run: |
          zip -r MpvVideo_macOS_Universal.zip MpvVideo_Package

      - name: 📤 Upload Plugin ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MpvVideo_WLX_Plugin
          path: MpvVideo_macOS_Universal.zip
